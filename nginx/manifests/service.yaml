apiVersion: v1
kind: Service
metadata:
  name: ngx-service
  namespace: ngx
  labels:
    app: nginx
spec:
  type: NodePort
  selector:
    app: nginx
  ports:
  - port: 8889
    targetPort: 80
    nodePort: 32100
